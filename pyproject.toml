[project]
name = "ines-claude-tools"
version = "0.1.0"
description = "Personal Claude Code plugin marketplace"
requires-python = ">=3.10"
dependencies = []
authors = [{ name = "Inês Pinheiro" }]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "hypothesis>=6.112.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
